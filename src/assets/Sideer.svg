<svg width="420" height="140"
     viewBox="0 0 420 140"
     xmlns="http://www.w3.org/2000/svg">

  <!-- Ring left half (grayish, thicker) -->
  <path d="M50 48
           A22 22 0 0 0 50 92"
        stroke="#9ca3af"
        stroke-width="7"
        fill="none"
        stroke-linecap="round"/>

  <!-- Ring right half (blackish, thicker) -->
  <path d="M50 48
           A22 22 0 0 1 50 92"
        stroke="#000000"
        stroke-width="7"
        fill="none"
        stroke-linecap="round"/>

  <!-- Diamond (slightly bigger) -->
  <polygon points="50,34 42,50 58,50"
           fill="#bfa14a"/>

  <!-- Text: Eng (black, bolder) -->
  <text x="95"
        y="88"
        font-family="Montserrat, Arial, sans-serif"
        font-size="56"
        font-weight="700"
        fill="#000000">
    Eng
  </text>

  <!-- Text: Wedding (white, bolder) -->
  <text x="175"
        y="88"
        font-family="Montserrat, Arial, sans-serif"
        font-size="56"
        font-weight="700"
        fill="#ffffff">
    Wedding
  </text>
</svg>
